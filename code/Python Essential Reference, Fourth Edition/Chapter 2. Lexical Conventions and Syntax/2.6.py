#!/usr/bin/env python3
# -*- coding: utf-8 -*-

# 2.6 Operators, Delimiters, and Special Symbols

# Operators:
# + - * ** / // % << >> & |
# ^ ~ < >  <= >= == != <> +=
# -= *= /= //= %= **= &= |= ^= >>= <<=

# delimiters
# ( ) [ ] { } , : . ` = ;

# equal(=) character serves as a delimiter
# comma(,) character is used to delimit arguments to a function
# period(.) is also used in floating-point numbers
# ellipsis(...) used in extended slicing operations.

# special symbols
# ' " # \ @
